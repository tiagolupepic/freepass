<%= link_to 'Editar', routes.edit_holiday_path(holiday.id), class: 'btn btn-sm btn-info' %>
<%= form_for :holiday, routes.holiday_path(holiday.id), class: 'form-buttons', method: :delete do
    submit 'Excluir', class: 'btn btn-sm btn-danger'
end %>
