<div class="page-title">
  <div class="title_left">
    <h3>Cartões</h3>
  </div>

  <div class="title_right">
    <div class="col-md-6 col-sm-6 col-xs-12 form-group pull-right top_search">
      <div class="input-group">
        <input type="text" class="form-control" placeholder="Filtrar por...">
        <span class="input-group-btn">
          <button class="btn btn-default" type="button">Buscar</button>
        </span>
      </div>
    </div>
  </div>
</div>

<div class='row'>
  <div class="col-md-12 col-sm-12 col-xs-12">
    <div class="x_panel">
      <div class="x_title">
        <h2>Listar</h2>
        <div class="clearfix"></div>
      </div>

      <div class="x_content">
      <% if cards.size > 0 %>
        <div class="table-responsive">
          <table class="table table-striped jambo_table bulk_action">
            <thead>
              <tr class="headings">
                <th class="column-title" style="display: table-cell;">Número </th>
                <th class="column-title" style="display: table-cell;">Usuário </th>
                <th class="column-title no-link last" style="display: table-cell;"><span class="nobr">Ações</span></th>
              </tr>
            </thead>

            <tbody>
              <% cards.each do |card| %>
                <tr class="even pointer">
                  <td class=" "><%= card.number %></td>
                  <td class=" "><%= card.full_name %></td>
                  <td class=" last">
                    <%= link_to 'Visualizar', routes.card_path(card.id), class: 'btn btn-sm btn-default' %>
                    <%= render partial: 'cards/buttons', locals: { card: card } %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <p>Nenhum cartão / RFID cadastrado</p>
      <% end %>
      </div>
    </div>
  </div>
</div>
