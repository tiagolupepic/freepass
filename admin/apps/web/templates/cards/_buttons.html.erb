<%= link_to 'Editar', routes.edit_card_path(card.id), class: 'btn btn-sm btn-info' %>
<%= form_for :card, routes.card_path(card.id), class: 'form-buttons', method: :delete do
    submit 'Excluir', class: 'btn btn-sm btn-danger'
end %>
