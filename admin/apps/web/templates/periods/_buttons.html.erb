<%= link_to 'Editar', routes.edit_period_path(period.id), class: 'btn btn-sm btn-info' %>
<%= form_for :period, routes.period_path(period.id), class: 'form-buttons', method: :delete do
    submit 'Excluir', class: 'btn btn-sm btn-danger'
end %>
